import React, { useState, useRef, useEffect } from "react";

import {
  CarouselButton,
  CarouselButtonDot,
  CarouselButtons,
  CarouselContainer,
  CarouselItem,
  CarouselItemImg,
  CarouselItemText,
  CarouselItemTitle,
  CarouselMobileScrollNode,
} from "./TimeLineStyles";
import {
  Section,
  SectionDivider,
  SectionText,
  SectionTitle,
} from "../../styles/GlobalComponents";
import { TimeLineData } from "../../constants/constants";



const Timeline = () => {
  const [activeItem, setActiveItem] = useState(0);
  const carouselRef = useRef();

  const scroll = (node, left) => {
    return node.scrollTo({ left, behavior: "smooth" });
  };

  const handleClick = (e, i) => {
    e.preventDefault();

    if (carouselRef.current) {
      const scrollLeft = Math.floor(
        carouselRef.current.scrollWidth * 0.7 * (i / TimeLineData.length)
      );

      scroll(carouselRef.current, scrollLeft);
    }
  };

  const handleScroll = () => {
    if (carouselRef.current) {
      const index = Math.round(
        (carouselRef.current.scrollLeft /
          (carouselRef.current.scrollWidth * 0.7)) *
          TimeLineData.length
      );

      setActiveItem(index);
    }
  };

  // snap back to beginning of scroll when window is resized
  // avoids a bug where content is covered up if coming from smaller screen
  useEffect(() => {
    const handleResize = () => {
      scroll(carouselRef.current, 0);
    };

    window.addEventListener("resize", handleResize);
  }, []);

  return (
    <Section id="about">
     <SectionTitle>Web Scraping & Backend Expertise</SectionTitle>
<SectionText>
I am a Full Stack Developer specialized in Web Scraping, API Integration, and
Backend Development . I build
robust, scalable, and secure data pipelines, automate data extraction from
various sources, and deliver high-performance solutions. Skilled in Node.js,
Rust, and cloud technologies, I focus on clean code, seamless user experiences,
and efficient data management to transform raw data into actionable insights.
</SectionText>

      <CarouselContainer ref={carouselRef} onScroll={handleScroll}>
    
      </CarouselContainer>
      <CarouselButtons>
        
      </CarouselButtons>
      <SectionDivider />
    </Section>
  );
};

export default Timeline;
